# stashy-ui

This project was generated by Forge.

## Start editing

- Update `app/[locale]/page.tsx` for the starter page
- Update `app/[locale]/layout.tsx` for route-aware document language and direction
- Update `proxy.ts` if you want to change how `/` chooses the initial locale
- Update `components/` for UI pieces
- Update `components/app-providers.tsx` for root provider composition

## Included by default

- Next.js App Router
- shadcn preset: `luma` (`b1VlIwYS`)
- Radix UI primitives
- light/dark theme toggle
- English/Arabic runtime direction toggle
- centralized sound-ready hook surface
- code quality: `Oxlint + Oxfmt`
- customized text selection and scrollbar styling

## Commands

```bash
pnpm run dev
pnpm run build
pnpm run typecheck
pnpm run lint
pnpm run format
```
