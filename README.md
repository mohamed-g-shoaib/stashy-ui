# Stashy UI — Agentic Web Sandbox

> ⚠️ **AI AGENTS & DEVELOPERS: START HERE**
> This repository is heavily documented and rigidly structured to support continuous LLM workflow.
> **DO NOT begin coding or scaffolding until you have read `spec/index.md`.**
> The `spec/index.md` file contains the master directory map for design rules (`DESIGN.md`), daily workflows (`sessions/`), and coding standards (`skills.md`).

---

## Technical Foundation

This staging environment was generated via Forge and is configured for rapid UI prototyping.

- **Framework:** Next.js App Router
- **Styling:** Tailwind CSS v4 + Radix UI primitives + shadcn preset (`luma`)
- **Theme:** Configured for light/dark parsing and RTL (English/Arabic directionality)
- **Code Quality:** `Oxlint + Oxfmt`
- **Core Hook:** Centralized sound-ready hook surface included by default

## Commands

```bash
pnpm run dev
pnpm run build
pnpm run typecheck
pnpm run lint
pnpm run format
```
